import { enableProdMode } from '@angular/core';
import { environment } from './environments/environment';
import { AppServerModule } from './app/AppServerModule';

if (true) {
  enableProdMode();
}

export { AppServerModule };

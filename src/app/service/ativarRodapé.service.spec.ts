/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AtivarRodapéService } from './ativarRodapé.service';

describe('Service: AtivarRodapé', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AtivarRodapéService]
    });
  });

  it('should ...', inject([AtivarRodapéService], (service: AtivarRodapéService) => {
    expect(service).toBeTruthy();
  }));
});
